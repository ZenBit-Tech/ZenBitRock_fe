export const metadata = {
    title: 'Change password',
  };
  
  type Props = {
    children: React.ReactNode;
  };
  
  export default function ChangePasswordLayout({ children }: Props): JSX.Element {
    return <>{children}</>;
  }