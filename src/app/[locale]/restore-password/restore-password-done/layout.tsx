export const metadata = {
  title: 'Restore password done',
};

type Props = {
  children: React.ReactNode;
};

export default function RestorePasswordDoneLayout({ children }: Props): JSX.Element {
  return <>{children}</>;
}
