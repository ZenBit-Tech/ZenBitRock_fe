export const metadata = {
  title: 'Restore password',
};

type Props = {
  children: React.ReactNode;
};

export default function RestorePasswordLayout({ children }: Props): JSX.Element {
  return <>{children}</>;
}
