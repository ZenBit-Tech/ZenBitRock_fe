export const metadata = {
  title: 'Verify code',
};

type Props = {
  children: React.ReactNode;
};

export default function VerifyCodeLayout({ children }: Props): JSX.Element {
  return <>{children}</>;
}
