export const metadata = {
    title: 'Reset password',
  };
  
  type Props = {
    children: React.ReactNode;
  };
  
  export default function ResetPasswordLayout({ children }: Props): JSX.Element {
    return <>{children}</>;
  }
  