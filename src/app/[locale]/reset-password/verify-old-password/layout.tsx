export const metadata = {
    title: 'Verify old password',
  };
  
  type Props = {
    children: React.ReactNode;
  };
  
  export default function VerifyOldPasswordLayout({ children }: Props): JSX.Element {
    return <>{children}</>;
  }
  